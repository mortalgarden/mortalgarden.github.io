{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "firefox",
            "request": "launch",
            "reAttach": true,
            "name": "Launch site",
            "url": "http://localhost:4000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}