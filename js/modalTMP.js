                              var dialog;
                              const body = document.querySelector("body");
function openCheck(dialog) {
  if (dialog.open) {
    body.classList.add('open-modal');
  } else {
    body.classList.remove('open-modal');
  }
};
// OIAR EMPLOYEES
                        const samamaModal = document.getElementById("SamamaKhalid");
function samamaShow() {  var dialog = samamaModal;
  samamaModal.showModal();
  openCheck(dialog);
}
function samamaClose() {  var dialog = samamaModal;

  samamaModal.classList.add('hide');

  samamaModal.addEventListener('webkitAnimationEnd', function() {
    samamaModal.classList.remove('hide');
    samamaModal.close();
    openCheck(dialog);
    samamaModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                        const aliceModal = document.getElementById("AliceDyer");
function aliceShow() {  var dialog = aliceModal;
  aliceModal.showModal();
  openCheck(dialog);
}
function aliceClose() {  var dialog = aliceModal;

  aliceModal.classList.add('hide');

  aliceModal.addEventListener('webkitAnimationEnd', function() {
    aliceModal.classList.remove('hide');
    aliceModal.close();
    openCheck(dialog);
    aliceModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                        const gwenModal = document.getElementById("GwenBouchard");
function gwenShow() {  var dialog = gwenModal;
  gwenModal.showModal();
  openCheck(dialog);
}
function gwenClose() {  var dialog = gwenModal;

  gwenModal.classList.add('hide');

  gwenModal.addEventListener('webkitAnimationEnd', function() {
    gwenModal.classList.remove('hide');
    gwenModal.close();
    openCheck(dialog);
    gwenModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                        const celiaModal = document.getElementById("CeliaRipley");
function celiaShow() {  var dialog = celiaModal;
  celiaModal.showModal();
  openCheck(dialog);
}
function celiaClose() {  var dialog = celiaModal;

    celiaModal.classList.add('hide');

    celiaModal.addEventListener('webkitAnimationEnd', function() {
    celiaModal.classList.remove('hide');
    celiaModal.close();
    openCheck(dialog);
    celiaModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                        const lenaModal = document.getElementById("LenaKelley");
function lenaShow() {  var dialog = lenaModal;
  lenaModal.showModal();
  openCheck(dialog);
}
function lenaClose() {  var dialog = lenaModal;

  lenaModal.classList.add('hide');

  lenaModal.addEventListener('webkitAnimationEnd', function() {
    lenaModal.classList.remove('hide');
    lenaModal.close();
    openCheck(dialog);
    lenaModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                        const colinModal = document.getElementById("ColinBecher");
function colinShow() {  var dialog = colinModal;
  colinModal.showModal();
  openCheck(dialog);
}
function colinClose() {  var dialog = colinModal;

  colinModal.classList.add('hide');

  colinModal.addEventListener('webkitAnimationEnd', function() {
    colinModal.classList.remove('hide');
    colinModal.close();
    openCheck(dialog);
    colinModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                        const bonzoModal = document.getElementById("MrBonzo");
function bonzoShow() {  var dialog = bonzoModal;
  bonzoModal.showModal();
  openCheck(dialog);
}
function bonzoClose() {  var dialog = bonzoModal;

  bonzoModal.classList.add('hide');

  bonzoModal.addEventListener('webkitAnimationEnd', function() {
    bonzoModal.classList.remove('hide');
    bonzoModal.close();
    openCheck(dialog);
    bonzoModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                          const mowbrayModal = document.getElementById("LadyMowbray");
function mowbrayShow() {  var dialog = mowbrayModal;
  mowbrayModal.showModal();
  openCheck(dialog);
}
function mowbrayClose() {  var dialog = mowbrayModal;

  mowbrayModal.classList.add('hide');

  mowbrayModal.addEventListener('webkitAnimationEnd', function() {
    mowbrayModal.classList.remove('hide');
    mowbrayModal.close();
    openCheck(dialog);
    mowbrayModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                        const teddyModal = document.getElementById("TeddyVaughn");
function teddyShow() {  var dialog = teddyModal;
  teddyModal.showModal();
  openCheck(dialog);
}
function teddyClose() {  var dialog = teddyModal;

  teddyModal.classList.add('hide');

  teddyModal.addEventListener('webkitAnimationEnd', function() {
    teddyModal.classList.remove('hide');
    teddyModal.close();
    openCheck(dialog);
    teddyModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                        const klausModal = document.getElementById("Klaus");
function klausShow() {  var dialog = klausModal;
  klausModal.showModal();
  openCheck(dialog);
}
function klausClose() {  var dialog = klausModal;

  klausModal.classList.add('hide');

  klausModal.addEventListener('webkitAnimationEnd', function() {
    klausModal.classList.remove('hide');
    klausModal.close();
    openCheck(dialog);
    klausModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                        const freddyModal = document.getElementById("FREDDY");
function freddyShow() {  var dialog = freddyModal;
  freddyModal.showModal();
  openCheck(dialog);
}
function freddyClose() {  var dialog = freddyModal;

  freddyModal.classList.add('hide');

  freddyModal.addEventListener('webkitAnimationEnd', function() {
    freddyModal.classList.remove('hide');
    freddyModal.close();
    openCheck(dialog);
    freddyModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                        const norrisModal = document.getElementById("NORRIS");
function norrisShow() {  var dialog = norrisModal;
  norrisModal.showModal();
  openCheck(dialog);
}
function norrisClose() {  var dialog = norrisModal;

  norrisModal.classList.add('hide');

  norrisModal.addEventListener('webkitAnimationEnd', function() {
    norrisModal.classList.remove('hide');
    norrisModal.close();
    openCheck(dialog);
    norrisModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                          const chesterModal = document.getElementById("CHESTER");
function chesterShow() {  var dialog = chesterModal;
  chesterModal.showModal();
  openCheck(dialog);
}
function chesterClose() {  var dialog = chesterModal;

  chesterModal.classList.add('hide');

  chesterModal.addEventListener('webkitAnimationEnd', function() {
    chesterModal.classList.remove('hide');
    chesterModal.close();
    openCheck(dialog);
    chesterModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                          const augustusModal = document.getElementById("AUGUSTUS");
function augustusShow() {  var dialog = augustusModal;
  augustusModal.showModal();
  openCheck(dialog);
}
function augustusClose() {  var dialog = augustusModal;

  augustusModal.classList.add('hide');

  augustusModal.addEventListener('webkitAnimationEnd', function() {
    augustusModal.classList.remove('hide');
    augustusModal.close();
    openCheck(dialog);
    augustusModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
// --------------
// RECURRING CHARACTERS
                      const inkModal = document.getElementById("Ink5oul");
function inkShow() {  var dialog = inkModal;
  inkModal.showModal();
  openCheck(dialog);
}
function inkClose() {  var dialog = inkModal;

  inkModal.classList.add('hide');

  inkModal.addEventListener('webkitAnimationEnd', function() {
    inkModal.classList.remove('hide');
    inkModal.close();
    openCheck(dialog);
    inkModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
                        const needleModal = document.getElementById("Needles");
function needleShow() {  var dialog = needleModal;
  needleModal.showModal();
  openCheck(dialog);
}
function needleClose() {  var dialog = needleModal;

  needleModal.classList.add('hide');

  needleModal.addEventListener('webkitAnimationEnd', function() {
    needleModal.classList.remove('hide');
    needleModal.close();
    openCheck(dialog);
    needleModal.removeEventListener('webkitAnimationEnd',  arguments.callee, false);
  }, false);
}
// --------------------